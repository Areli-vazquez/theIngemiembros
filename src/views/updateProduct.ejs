<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('partials/header')%>
  </head>
  <body>
    <%- include('partials/navigation')%>
    <div class="fondo">
      <div class="container p-4">
        <form method="POST" action="/updateProduct" class="card card-body trans">
          <% const {id, name, description, price, image } = product[0] %>
          <div class="form-group">
            <label for="id"></label>
            <input type="hidden" class="form-control" name="id" value="<%= id %>" />
          </div>
          <div class="form-group">
            <label for="name">HOTEL</label>
            <input type="text" class="form-control" name="name" value="<%= name %>" required/>
          </div>
          <div class="form-group">
            <label for="description">RESEÑA BREVE DEL HOTEL:</label>
            <input type="text" class="form-control" name="description" value="<%= description %>" required/>
          </div>
          <div class="form-group">
            <label for="price">PRECIO</label>
            <input type="text" class="form-control" name="price" value="<%= price %>" required/>
          </div>
          <div class="form-group">
            <label for="image">IMAGEN</label>
            <input type="text" class="form-control" name="image"  value="<%= image %>" required/>
          </div>
          <input type="submit" class="btn btn-primary btn-block" value="Save">
        </form>
      </div>
    </div>
<%- include('partials/footer')%>
  </body>
</html>
